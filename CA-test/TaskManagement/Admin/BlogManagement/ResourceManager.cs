namespace TaskManagement.Admin.BlogManagement
{
    public class ResourceManager
    {
        private Type type;

        public ResourceManager(Type type)
        {
            this.type = type;
        }

        internal object GetType(string v)
        {
            throw new NotImplementedException();
        }
    }
}